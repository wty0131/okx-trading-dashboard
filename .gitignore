# Python
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# 密钥（绝不提交）
.env
# 保存 API Key 时自动生成的备份与临时文件 —— 内容与 .env 相同（含真实密钥），
# 必须一并忽略，否则一次「保存并测试」就会把密钥带进版本库。
.env.bak
.env.tmp
*.key

# 数据与状态
data/*.db
data/*.sqlite3
data/cache/
data/paper_state/
*.parquet

# 运行产物
logs/*.log
output/
.DS_Store
Thumbs.db

data/paper_state_1h/
docs/*.csv

# 实盘当日累计下单金额（运行时状态，不含密钥）
data/trade_usage.json

